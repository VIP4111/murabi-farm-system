# نشر "مربي" على سيرفر اختبار

هذا الملف خطوات عملية لنشر التطبيق على منصة استضافة مجانية، باستخدام
ملفات النشر الجاهزة بجذر المشروع (`Dockerfile`, `Procfile`, `.env.example`).
لا تحتاج تعديل أي كود — فقط ربط حساب استضافة بمستودع Git يحتوي هذا المجلد.

## الخطوة صفر: رفع المشروع لمستودع Git (لازمة لأي منصة)

المشروع حالياً ليس Git repo. من جذر المجلد:

```bash
git init
git add .
git commit -m "نشر أولي لبيئة الاختبار"
```

ثم أنشئ مستودعاً فارغاً على GitHub/GitLab وارفعه:

```bash
git remote add origin <رابط المستودع>
git push -u origin main
```

## الخيار أ — Render (مجاني، الأبسط)

1. سجّل دخول إلى [render.com](https://render.com) واربط حساب GitHub/GitLab.
2. **New → Web Service** → اختر المستودع اللي رفعته.
3. Render يكتشف `Dockerfile` تلقائياً — اتركه كما هو (Environment: Docker).
4. **Instance Type**: Free.
5. بعلامة تبويب **Environment**، أضف المتغيرات التالية (انسخها من `.env.example` وغيّر القيم):
   - `SECRET_KEY` = قيمة عشوائية طويلة (مثلاً أنشئها بـ`python -c "import secrets; print(secrets.token_hex(32))"`)
   - `OWNER_PHONE` = رقم جوال تختاره لحساب الدخول
   - `OWNER_PASSWORD` = كلمة مرور تختارها
   - `OWNER_NAME` = اسم يظهر بالواجهة (اختياري)
6. **Create Web Service** — أول بناء يأخذ 3-5 دقائق. Render يعطيك رابطاً تلقائياً بالشكل:
   `https://<اسم-الخدمة>.onrender.com`

**تنبيه مهم بالخطة المجانية**: لا يوجد قرص دائم (Persistent Disk) بالخطة
المجانية — قاعدة SQLite تُصفَّر عند كل إعادة نشر أو إعادة تشغيل الخدمة
(تنام تلقائياً بعد 15 دقيقة خمول وتوقظ عند أول زيارة). لو تحتاج البيانات
تبقى بين الجلسات: أنشئ **PostgreSQL** مجاني من نفس لوحة Render (New →
PostgreSQL)، وأضف رابطه كمتغيّر `DATABASE_URL` بالخدمة — التطبيق يتحول
فوراً بدون أي تعديل كود.

### ⚠️ الملفات المرفوعة (صور بلاغات، ملاحظات صوتية، فواتير) — بند إضافي 77

نفس مشكلة SQLite بالضبط: بدون قرص دائم، أي ملف يرفعه مستخدم يضيع عند
كل إعادة نشر. حلّان، اختر واحد منهم:

1. **الأبسط — PostgreSQL بس، بدون حل للملفات بعد**: لو ما تحتاج حفظ
   الصور/الملفات الصوتية بشكل دائم حالياً (بس تبي البيانات الأساسية
   تبقى)، فعّل PostgreSQL فقط (الخطوة أعلاه) وتجاهل قسم الملفات هذا —
   الملفات تستمر تضيع عند كل إعادة نشر، بس بيانات النظام (حيوانات،
   مهام، مالية) تبقى محفوظة.
2. **الأشمل — قرص دائم (Persistent Disk، يحتاج خطة مدفوعة بـRender،
   Starter فأعلى)**:
   - من لوحة الخدمة: **Disks → Add Disk** → اختر مساراً مثلاً `/var/data`
     وحجماً (1GB كافي للبداية).
   - أضف متغيّر بيئة جديد: `UPLOAD_DIR` = `/var/data/uploads`.
   - أعد النشر — من الآن، كل ملف جديد يُرفع يُخزَّن بالقرص الدائم
     ويبقى بين عمليات إعادة النشر. **الملفات القديمة (المرفوعة قبل هذا
     التغيير) ما تنتقل تلقائياً** — لو تحتاج نقلها يدوياً، تواصل معايا.
   - لو تبي نفس القرص الدائم يحمي قاعدة SQLite أيضاً بدل PostgreSQL:
     غيّر `DATABASE_URL` إلى `sqlite:////var/data/farm_system.db` (أربع
     شرطات مائلة — المسار المطلق). الأفضل غالباً تستخدم PostgreSQL
     للبيانات والقرص الدائم للملفات معاً، مو أحدهما بس.

## الخيار ب — Railway (مجاني بحد استخدام شهري)

1. سجّل دخول إلى [railway.app](https://railway.app) واربط GitHub.
2. **New Project → Deploy from GitHub repo** → اختر المستودع.
3. Railway يستخدم `Procfile` تلقائياً (أو `Dockerfile` لو فضّلته من Settings → Deploy).
4. من تبويب **Variables** أضف نفس المتغيرات المذكورة أعلاه (`SECRET_KEY`, `OWNER_PHONE`, `OWNER_PASSWORD`).
5. من **Settings → Networking**، فعّل **Generate Domain** — يعطيك رابطاً بالشكل:
   `https://<اسم-عشوائي>.up.railway.app`

نفس تنبيه القرص الدائم أعلاه ينطبق — أضف Postgres من **New → Database → PostgreSQL**
داخل نفس المشروع لو تحتاج بقاء البيانات.

## بعد النشر: تسجيل الدخول

الحساب الأول يُنشأ تلقائياً عند أول إقلاع (`flask seed` يشتغل تلقائياً
قبل السيرفر، عبر `Dockerfile`/`Procfile release`) — بنفس القيم اللي
حطّيتها بمتغيرات `OWNER_PHONE`/`OWNER_PASSWORD` بالخطوة أعلاه. افتح
الرابط اللي أعطتك المنصة، وسجّل دخول برقم الجوال وكلمة المرور اللي اخترتها.

## بعد ما يصير عندك رابط فعلي

ابعثه لي هنا وأقدر أختبره مباشرة من هذي الجلسة (المتصفح المدمج يفتح
أي رابط عام)، أو أكمل معك أي خطوة تعثّرت بها بالنشر نفسه.
