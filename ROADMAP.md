# خارطة طريق نظام إدارة الحلال — الحالة الفعلية

آخر تحديث: 2026-07-23 (بعد دعم اللغات الميدانية — بند 44 إضافي، آخر بند من ثلاثة بمراجعة الجاهزية الميدانية — كلها مكتملة الآن)

هذا الملف هو مرجع دائم لوش تم بناؤه فعلياً ووش لسا باقي، حسب خارطة الطريق
ذات الـ11 مرحلة (0-10) اللي اتفقنا عليها. حدّثه كل ما تغيّر شي — لا تعتمد
على الذاكرة أو محادثة قديمة.

## القرار التقني (مرحلة 0)

- ✅ **الخادم**: Flask (Python) — كما هو مقرر.
- 🟡 **قاعدة البيانات**: SQLite للتطوير حالياً. الكود جاهز للتحويل لـ
  PostgreSQL بتغيير `DATABASE_URL` بملف `.env` فقط (سطر واحد، بدون أي
  تعديل كود) — لسا ما انتقلنا فعلياً لـ Postgres حقيقي.
- ✅ **PWA / أوف لاين**: مبني (بند 41، 2026-07-23) وقيد التوسيع لكل
  النظام (بند 53، 2026-07-25) — Service Worker + IndexedDB، طابور
  محلي يمزامن تلقائياً عند رجوع الاتصال، تثبيت PWA. راجع بند 53 أدناه.
- ❌ **إشعارات واتساب/فورية**: غير مبنية.
- ❌ **تخزين سحابي منفصل للصور/الصوت**: غير مبني — أي مسار صورة حالياً
  حقل نص (URL) يُدخل يدوياً، مافيه رفع فعلي للملفات.

## حالة المراحل

| # | المرحلة | الحالة | التفاصيل |
|---|---|---|---|
| 0 | القرار التقني والبنية | ✅ منجزة | Flask + SQLAlchemy + Flask-Migrate، مخطط قاعدة بيانات علائقي نظيف (مو شيتات) |
| 1 | الأساس: حسابات وأدوار | ✅ منجزة | دخول حقيقي لكل مستخدم، أدوار قابلة للتوسّع بالكامل من الواجهة (إنشاء دور + تحديد صلاحياته)، صفحة إعدادات للمالك حصراً بخدمات قابلة للتفعيل |
| 2 | العمليات الأساسية | 🟡 معظمها منجز | حيوانات (نقطة دخول موحّدة)، صيدلية، أطباء، زيارات بيطرية، أمراض، تطعيمات، مالية (كاملة + محدودة للدكتور). **ناقص**: نظام "خيارات موحّدة ومترجمة" (قوائم أعراض/أنواع بلاغ قابلة للتوسيع من المالك ومترجمة تلقائياً) — الاختيارات حالياً مكتوبة يدوياً بكل قالب |
| 3 | محرك دورة الإنتاج | 🟡 منجز بفارق معماري | 10 مراحل + 6 مسارات + بوابات صارمة (تمنع البيع/الأرشفة فعلياً) + سجل أحداث كامل (CycleEvent). **الفارق**: البوابات مكتوبة كدوال بايثون (`app/core/cycle_engine.py`)، مو كجدول بيانات يعدّله المالك بدون كود — قرار معماري يستاهل نقاش لو "بدون كود" شرط أساسي |
| 4 | الولادة والعزل التلقائي | ✅ منجزة (بحدود) | تسجيل ولادة يفعّل تلقائياً: نقل الأم والمولود لأول حظيرة `barn_type=عزل`، 7 مهام فحص يومي، مهمة دكتور خلال 48 ساعة، مهمة وزن، مهمتي تحصين (أم+مولود)، مهمة تبديل علف (يدوية بس — العلف مو مبني). بوابة خروج العزل بمحرك الدورة تتطلب فعلياً: فحص بيطري + تحصين للمولود، وتحصين للأم بعد تاريخ آخر ولادة. **قيم افتراضية اجتهادية** (7 أيام عزل، 48 ساعة، 21 يوم تحصين) موثّقة بـ`app/core/isolation_service.py` — عدّلها لو عندك أرقام مختلفة. اختُبرت فعلياً وتأكدت إن البوابة تتحرك صح لما تتوفر الأدلة |
| 5 | تطبيق العامل (أوف لاين) | ✅ منجزة (وُسِّعت لكل النظام ببند 53) | **واجهة مبسّطة (بند 27)**: `worker_home.html` بـ5 أزرار كبيرة لدور "العامل" تحل محل اللوحة العامة تلقائياً. **أوف لاين حقيقي (بند 41، 2026-07-23)**: PWA خفيف (Service Worker + IndexedDB)، بدأ مقصوراً على 8 شاشات إدخال ميداني لعامل/طبيب/ممرض. **توسيع كامل (بند 53، 2026-07-25)**: صار يشمل كل شاشات النظام (بما فيها العمليات الجماعية والمالية) ولكل الأدوار، مع نسخة محفوظة مؤشَّرة للتقارير — بعد إصلاح ثغرة أمان بيانات حقيقية بآلية اكتشاف نجاح/فشل المزامنة (راجع بند 53). **دعم 4 لغات (2026-07-23)**: راجع بند 44. راجع بنود 41، 44، 53 بـ`MASTER_SPEC.md` للتفاصيل الكاملة |
| 6 | البلاغات ومهام الدكتور الذكية | 🟡 معظمها منجز | **البلاغات**: مبنية بالكامل ومختبرة (جديد→مقبول/مؤجل/ملغى→منفّذ بانتظار مراجعة→مغلق). **المهام الذكية**: النظام الأساسي مبني ومختبر (`Task` model + `task_service.py`) — توليد تلقائي (حالياً من العزل فقط)، مراجعة الدكتور (موافقة/تأجيل/حذف)، صندوق مراجعة المالك (استرجاع/حذف نهائي)، بدء/إنجاز من العامل مع دعم "يتطلب صورة". **ناقص**: توليد مهام ذكية من مصادر ثانية غير العزل (تطعيمات مستحقة عامة، أوزان متأخرة...) |
| 7 | ذكاء العلف | ✅ منجزة (بحدود) | قاعدة بيانات مكوّنات علف (بروتين/طاقة/ألياف/كالسيوم/فسفور من ملصق الكيس)، وصفات مركّبة بنسب مئوية مع حساب تلقائي للقيمة الغذائية الموزونة والتكلفة، حاسبة احتياج يومي (وزن الحيوان الفعلي × حالته الفسيولوجية المخمّنة تلقائياً من محرك الدورة)، ترشيح أرخص وصفة مطابقة مع فحص كفاية المخزون، حركة مخزون (وارد/صادر) بخصم تلقائي، تنبؤ بالنفاد من معدل الاستهلاك الفعلي. **تنبيه مهم موثّق بالكود** (`app/feed/feed_service.py`): أرقام الاحتياج (2.5% من وزن الجسم، مضاعفات النمو/الحمل/الرضاعة) تقديرات عامة مبسّطة للمجترات الصغيرة، **مو توصية بيطرية معتمدة** — لازم تُراجَع مع طبيبك قبل الاعتماد عليها بقرارات شراء فعلية. اختُبرت فعلياً (مكوّن→وصفة→حاسبة→ترشيح→خصم مخزون→تقدير نفاد) وكل الأرقام طلعت صحيحة |
| 8 | التكاثر المتقدم + النعام | 🟡 التكاثر منجز، النعام لا | **التكاثر**: تلقيح، تشخيص حمل، سونار، برامج شياع توأمي (أجهزة + حقن هرمونية + محاولات) — مبنية ومختبرة. حقول برنامج الشياع اختُصرت من 62 حقل بالنظام القديم لـ~15 حقل فعّال (قابلة للتوسيع لو احتجت حقل معيّن). **النعام**: غير مبنية إطلاقاً (مؤكَّد إنها لسا مطلوبة) |
| 9 | وحدات الشركة العالمية | ❌ غير مبنية | لا أنساب/قيمة وراثية، لا CRM، لا ذكاء سوق، لا تقارير مستثمر، لا تعدد فروع، لا إدارة موردين، لا HR، لا جواز سفر QR للحيوان. (ملاحظة: أزرار تفعيل CRM/تعدد الفروع/الأنساب/الموردين موجودة بصفحة الإعدادات من الأساس كخدمات اختيارية، لكن بدون أي وظيفة فعلية خلفها بعد) |
| 10 | الجودة والترحيل والإطلاق | 🟡 جزئية | ✅ نسخ احتياطي محلي فعلي (بند 34، SQLite فقط — يحتاج أداة مستوى قاعدة بيانات حقيقية لإنتاج PostgreSQL) و✅ فحص جاهزية آلي قبل النشر (بند 33). **الباقي غير مبني**: لا اختبارات آلية حقيقية (الاختبار لين الآن يدوي عبر المتصفح وطلبات HTTP مباشرة)، لا ترحيل بيانات من الشيتات القديمة، لا تجربة تجريبية بفريق محدود |

## فجوات إضافية اكتشفناها وسدّيناها أثناء البناء

هذي ماكانت مذكورة صراحة بالخارطة، لكن لقيناها فجوات حقيقية (صلاحية معرّفة
بدون شاشة فعلية) وبنيناها كجزء من المراحل أعلاه:

- ✅ شاشة "تسجيل حيوان جديد" (كانت الصلاحية `animals.manage` بدون شاشة)
- ✅ شاشة "إضافة عضو فريق" (كانت `users.manage` بدون شاشة)
- ✅ شاشة "إنشاء وتعديل دور وصلاحياته" (كانت `roles.manage` بدون شاشة)
- ✅ تعريب حالات النظام (نشط/مباع/نافق...) بدل ظهورها بالإنجليزي
- ✅ شاشة مالية مصغّرة للدكتور (`finance.health.view` كانت بدون شاشة)
- ✅ شاشة "إدارة الحظائر" (كانت `barns.manage` بدون شاشة، ولازمة لتوجيه المهام حسب الحظيرة)
- ✅ صفحة "تفاصيل الرأس الشاملة" (`/animals/<id>`) — تجمع كل سجلات حيوان
  واحد (بيطري، أمراض، تحصينات، تكاثر، ولادات، أوزان، ملاحظات، مالية)
  بتبويبات + تسلسل زمني موحّد، بدل تفتيشها بجداول منفصلة (بند 9 بالمواصفة).
  أضفنا معها جدولين ناقصين (سجل وزن تاريخي، ملاحظات حرة) — الحليب أُجّل.
- ✅ قوائم الحلال المفلترة (بند 8) — فلاتر بعدّادات على نفس شاشة
  `/animals` (البهم، الذكور، قريب الولادة، المقرعات، دافع/غير دافع،
  المرضعات، التسمين، النفوق). المصطلحات ماكانت معرّفة رقمياً بالمواصفة
  الأصلية، فحطينا تعريفات اجتهادية موثّقة بالكود وبالواجهة — راجع بند 8
  بـ`MASTER_SPEC.md` للتفاصيل الكاملة، وأكّد لي تعريف "دافع/غير دافع"
  تحديداً لو عندك معيار مختلف.
- ✅ إعدادات تناسل الأم + فلتر "أمهات جاهزة للتقريع" (بند 10.4) — إعدادان
  جديدان بشاشة الإعدادات (أقل عمر تقريع أول، أقل فترة راحة بعد الولادة)
  + فلتر تاسع بنفس شاشة `/animals`. راجع بند 10 بـ`MASTER_SPEC.md`
  للتفاصيل.
- ✅ قائمة تحقق الولادة (بند 11) — جدول `BirthRecord` بحقول منفصلة
  (تنفّس، وقوف، لبأ، تعقيم سرة، عيوب خلقية)، يظهر بتبويب "ملخص" بصفحة
  تفاصيل الرأس لأي مولود بالمزرعة.
- ✅ سجل الحليب (بقية بند 14) — جدول `MilkRecord` (تاريخ/جلسة/كمية)،
  تبويب "الحليب" بصفحة تفاصيل الرأس صار فيه فورم + سجل فعلي بدل الإشعار
  المؤقت. بند 14 صار مكتمل بالكامل (وزن + ملاحظات + حليب).
- ✅ محرك العمليات الجماعية (بند 17) — تم بالكامل (2026-07-23). نفس
  محرك اختيار متعدد + إجراء واحد (`app/core/bulk_service.py` +
  `bulk_action_form.html`) وسّعناه من أربع لـ**تسع عمليات**: وزن،
  تحصين، ملاحظة، نقل حظيرة (الأصلية) + بيع (محمي ببوابة الدورة)، تسجيل
  نفوق (بدون بوابة)، علاج/مرض (بحارس سلامة بيانات بند 45)، عزل (نقل
  تلقائي لحظيرة العزل)، فحص سونار (نتيجة مستقلة لكل رأس). زودنا كمان
  شاشة "شراء دفعة جديدة" منفصلة (`/animals/bulk-purchase`) لإنشاء رؤوس
  جديدة دفعة واحدة. التغذية الجماعية ما احتاجت دالة جديدة — `FeedMovement`
  أصلاً حركة بمستوى الحظيرة الكاملة، فهي جماعية أصلاً. اختُبرت التسع
  عمليات فعلياً (تفاصيل بند 17 بـ`MASTER_SPEC.md`). راجع بند 17
  بـ`MASTER_SPEC.md` للتفاصيل الكاملة.
- ✅ فجوات المالية الثلاث (بند 18) — الشراء يسجّل حركة مالية تلقائياً
  (الهدية/الرصيد الافتتاحي لا)، دعم خارجي كدين + سداد (منفصل عن الداخل/
  الخارج التشغيلي)، تقرير تكلفة الرأس الشهرية (`/finance/monthly-cost-report`).
  راجع بند 18 بـ`MASTER_SPEC.md` للتفاصيل والتنبيه الصادق حول دقة عدد
  الرؤوس بالأشهر الماضية.
- ✅ محرك البيع الذكي (بند 19) — بنيناه حسب قواعدك بالضبط: الذكور حسب
  العمر (بيع عادي 6 أشهر، أضاحي فوق 6 أشهر — بحثنا السن الشرعي فعلياً
  وأكدنا 6 أشهر كاملة حد أدنى للجذعة من الضأن) + وزن ثانوي، الإناث نظام
  علامات مباشر (تأخر حمل، لم تحمل إطلاقاً، ترفض الرضاعة، ضرع تالف) —
  أي علامة = بيع فوري. شاشة `/animals/smart-sale`. اختُبر بسبع حالات
  وطلعت كلها صحيحة. راجع بند 19 بـ`MASTER_SPEC.md` للتفاصيل الكاملة.
- ✅ شاشة التنبيهات (بند 20) — `/alerts` + بطاقة بالرئيسية + رابط بالقائمة
  الجانبية. ثمانية مصادر (السبعة الأصلية + حيوانات جاهزة للبيع من بند 19).
  حقل `alert_before_days` (كان موجود بدون استخدام) صار يقود المنطق فعلياً.
  اختُبرت الثمانية مصادر بسبع حالات تجريبية وطلعت كلها صحيحة. راجع بند
  20 بـ`MASTER_SPEC.md` للتفاصيل.
- ✅ تسلسل المهام (بند 21) — حقل `Task.depends_on_task_id`، مهمة تابعة
  تظهر "🔒 مقفلة" للعامل لين السابقة تصير منجزة. الحماية على مستوى
  الخدمة (`task_service.py`) مو بس الواجهة. اختُبر فعلياً: رُفض بدء
  مهمة تابعة، وبعد إنجاز السابقة صارت قابلة للبدء تلقائياً.
- ✅ وحدة التقارير التحليلية (بند 22) — بلوبرنت جديد `app/reports/`
  بأربع تقارير (شامل، نفوق، ولادات وإنتاج، مبيعات ومالية)، فلاتر تاريخ
  (اليوم/7 أيام/الشهر/مخصص)، استعلامات تجميع (aggregation) بمستوى قاعدة
  البيانات، وتصدير PDF (خط عربي مُرفَق `NotoNaskhArabic`) وExcel. صلاحية
  جديدة `analytics.view`. اختُبرت الأربعة تقارير بأرقام مطابقة 100%
  لحسابات يدوية، والتصدير 8 ملفات (4 تقارير × 2 صيغة) بمعاينة بصرية
  فعلية للـPDF أكّدت صحة النص العربي. راجع بند 22 بـ`MASTER_SPEC.md`.
- ✅ وحدة النعام والتفقيس (بند 23) — رجعت مطلوبة بقرار المستخدم
  (2026-07-23)، بُنيت End-to-End: حقل `Animal.species` على نفس جدول
  الحيوانات الموحّد (يعيد استخدام كل الوحدات العامة: حظائر/صحة/مالية/
  أوزان بدون تعديل)، جدول `OstrichEgg` لإنتاج البيض، جدول `Incubator`
  للحاضنات، ربط تلقائي بيضة→فرخ عند تسجيل الفقس (`app/core/ostrich_service.py`).
  قرار معماري: النعام **لا يدخل محرك دورة الإنتاج** إطلاقاً (مبني على
  بيولوجيا المجترات) — تأكّد فعلياً باختبار إن ما يُنشأ أي `ProductionWorkflow`
  له. فلاتر بند 8 التكاثرية تستثنيه صراحة. اختُبر End-to-End (بالغين+
  حاضنة+بيضتين، وحدة فقست ووحدة فشلت) وطابقت النتائج المتوقعة تماماً.
  راجع بند 23 بـ`MASTER_SPEC.md` للتفاصيل الكاملة.
- ✅ المساعد الذكي التفاعلي (بند 25) — بلوبرنت جديد `app/assistant/`
  بمعمارية هجينة بقرار المستخدم (2026-07-23): محرك محلي فوري (كلمات
  مفتاحية عربية مطبّعة + قراءة حية من الـDB + قاعدة معرفة تشغيلية/بيطرية
  ثابتة)، وجسر ترقية جاهز (`llm_bridge.py`) يفعّل Claude API تلقائياً بس
  بإضافة `ANTHROPIC_API_KEY` بـ.env، بدون أي تعديل واجهات/قوالب/جداول.
  جدول جديد `AssistantMessage` (سجل محادثة مستمر لكل مستخدم)، صلاحية
  جديدة `assistant.use` (ممنوحة افتراضياً لكل الأدوار الستة). كل نية
  محلية مربوطة بصلاحية محددة (نفس نمط الشاشات العادية). اختُبر End-to-End
  فعلياً بالمتصفح (بيانات حية، قاعدة معرفة، fallback، صلاحيات، استمرارية
  بعد إعادة تحميل) ونظّفنا بيانات الاختبار بعدها. راجع بند 25
  بـ`MASTER_SPEC.md` للتفاصيل الكاملة. بند 24 (رفض صريح) تخطّيناه بدون شغل.
- ✅ واجهة العامل المبسّطة (بند 27) — دور "العامل" يشوف `worker_home.html`
  (5 أزرار كبيرة) بدل اللوحة العامة تلقائياً عند فتح الرئيسية. أربعة أزرار
  (فحص/حالة صحية، نقل للعزل، تغذية، بيض/حضانة نعام) تفتح شاشة موحّدة
  (`team.worker_quick_report`) فيها: مهام العامل المفتوحة من نفس التصنيف
  (بدء/إنجاز يعيد استخدام `task_service.py` بالكامل، بما فيها قفل تسلسل
  المهام) + فورم بلاغ سريع بنوع مقفل (يعيد استخدام `report_service.py`).
  الزر الخامس يرجّع مباشرة لشاشة `/team/tasks` الموجودة. **بدون أي صلاحية
  جديدة** — قرار مقصود إن العامل يبلّغ بس، مو يكتب مباشرة على بيانات
  حساسة (تحويل حظيرة فعلي، تسجيل بيض فعلي...)، ونفس الفصل المستخدم بكل
  النظام بين الإبلاغ والتنفيذ. اختُبر End-to-End فعلياً: حساب عامل تجريبي
  + مهمتين تابعتين لبعض (تسلسل) + بلاغ فعلي، وتأكّدنا إن القفل يشتغل صح،
  البلاغ يوصل لصندوق الدكتور بالنوع الصحيح، حساب المالك ما تأثّر، ومحاولة
  فتح مسار محمي (`/animals`) من حساب العامل رجعت 403. نظّفنا كل بيانات
  الاختبار بعدها. راجع بند 27 بـ`MASTER_SPEC.md` للتفاصيل الكاملة.
- ✅ إكمال بلاغ العامل ومسار الطبيب (بند 28) — سدّينا الفجوتين الموثّقتين:
  (1) تعبئة تلقائية لمعلومات الحيوان (النوع/الجنس/العمر/الحظيرة/الحالة)
  عند اختيار الحيوان بنموذج البلاغ (نقطة نهاية JSON جديدة
  `GET /animals/<id>/quick-info` + JS بسيط، بشاشتي البلاغ العادية
  والمبسّطة معاً)، و(2) تسجيل صوتي فعلي (`MediaRecorder`) بديل عن الكتابة
  — أول رفع ملفات حقيقي بالمشروع (`report_service.save_voice_note`،
  تخزين محلي بـ`app/static/uploads/audio/`، تحقق امتداد/حجم، `MAX_CONTENT_LENGTH`
  جديد بالإعدادات). شاشة تفاصيل البلاغ صارت تعرض مشغّل صوت + معلومات
  الحيوان المختصرة. اختُبر End-to-End (تعبئة تلقائية فعلية بالمتصفح، رفع
  ملف صوتي فعلي وتحقق من حفظه وتشغيله، رفض بصمت لملف غير صالح، تعامل
  أنيق مع رفض إذن المايكروفون) ونظّفنا كل بيانات وملفات الاختبار بعدها.
  راجع بند 28 بـ`MASTER_SPEC.md` للتفاصيل الكاملة.
- ✅ إغلاق بند 26 (قواعد UI/UX العامة) — بناءً على طلب المستخدم بعد تراكم
  تحقق عملي كافٍ عبر شاشات حقيقية (بنود 20-28) بنفس نظام التصميم الموحّد
  بدون أي كسر. تنبيه صادق موثّق ببند 26 بـ`MASTER_SPEC.md`: هذا تحقق
  متراكم عبر شاشات حقيقية، مو جولة QA رسمية على أجهزة فعلية متعددة.
  كل البنود 1-25 و27-28 منجزة، وبند 26 مُغلق — راجع القائمة المرقّمة آخر `MASTER_SPEC.md`.
- ✅ مراجعة صلاحيات الأدوار النهائية (بند 29) — مراجعة أمنية شاملة لكل
  الـ112 route عبر 10 بلوبرنت مقابل الأدوار الستة، بمطابقة كل كود صلاحية
  مُستخدم بالكود ضد `permissions_registry.py` المركزي. **ثغرتان حقيقيتان
  أُصلحتا**: (1) `GET /animals/<id>/quick-info` (بند 28) ماكان فيها فحص
  صلاحية إطلاقاً — أي دور مخصّص مستقبلي بدون `animals.view`/`reports.submit`
  كان يقدر يجيب بيانات أي حيوان بتخمين رقمه، (2) `GET /team/reports/<id>`
  (الأخطر) — أي مستخدم مسجّل دخول كان يقدر يفتح تفاصيل أي بلاغ بالنظام
  (معلومات حساسة محتملة) بغض النظر عن علاقته فيه؛ صار مقصوراً على
  الرافع/الدكتور المستلم/المنفّذ المحوَّل له/حامل `reports.manage`.
  الأدوار الستة الافتراضية ما كانت متأثرة عملياً بالثغرتين (كل دور يملك
  صلاحية كافية أصلاً)، لكنهما ثغرتان حقيقيتان لأي دور مخصّص يُنشأ لاحقاً
  أو لأي مستخدم يخمّن رقم بلاغ بالرابط مباشرة. **فحص تراكب** 78 تركيبة
  (6 أدوار × 13 مسار عبر كل الوحدات) طابقت 100% تعريفات السجل المركزي
  بدون استثناء. وثّقنا كمان أربع صلاحيات مسجّلة بدون أي مسار يفحصها فعلياً
  (`audit.view` — ما فيه شاشة سجل تدقيق، `medical_options.manage` — يطابق
  فجوة بند 2 الموثّقة، `gates.approve` — بوابات المحرك تلقائية مو يدوية،
  `tasks.view_own` — زخرفية، السلوك يصير أصلاً بدونها) — ليست ثغرات أمنية،
  توثيق صادق فقط، تركناها بدون لمس لأنها قرار تصميم مو إصلاح عاجل. اختُبر
  الكل بحسابات فعلية لستة أدوار + دور مخصّص سابع "مجرّد" من كل الصلاحيات،
  ونظّفنا كل بيانات الاختبار بعدها بالكامل. راجع بند 29 بـ`MASTER_SPEC.md`
  للتفاصيل الكاملة.

- ✅ إغلاق القسم الرابع بالكامل (بندا 33 و34) — شاشة فحص جاهزية آلية
  قبل النشر (`app/core/readiness_service.py` + `/settings/readiness`،
  تسع نقاط تحقق حية: كلمة مرور المالك الافتراضية، `SECRET_KEY`، نوع
  قاعدة البيانات، حظيرة العزل، حيوانات بدون حظيرة/تاريخ ميلاد، حجم
  الفريق، الصيدلية، وجود نسخة احتياطية)، ونظام نسخ احتياطي محلي فعلي
  (`app/core/backup_service.py` + `/settings/backup` — إنشاء/عرض/تنزيل
  لملف SQLite، حماية من path traversal، تنبيه صادق إن هذا يشتغل بس
  لبيئة SQLite ومو بديل عن أدوات نسخ احتياطي حقيقية لإنتاج PostgreSQL).
  كمان سدّينا فجوة "صلاحية بدون شاشة" اللي اكتشفناها بمراجعة بند 29
  (`audit.view`) — شاشة `/settings/audit` جديدة تعرض آخر 200 حدث بسجل
  `AuditLog` الموجود أصلاً. اختُبر الكل فعلياً (إنشاء نسخة + تنزيلها،
  رفض path traversal، ظهور الحدث بسجل التدقيق فوراً، رفض وصول حساب
  عامل تجريبي للثلاث شاشات) ونظّفنا بيانات وملفات الاختبار بعدها بالكامل.
  راجع بندي 33 و34 بـ`MASTER_SPEC.md` للتفاصيل الكاملة. بند 31 📌 غير
  منطبق هيكلياً من الأساس، وبندا 30/32 كانا ✅ منجزين من قبل. القسم
  الخامس (35-40) مرجع تاريخي فقط، لا شغل بناء منتظر — قواعده العامة
  (بند 39) متّبعة فعلاً طوال المشروع.

## 🎉🎉 الوثيقة كاملة — البنود 1-40 مكتملة بنسبة 100%

## بنود إضافية بعد اكتمال الوثيقة (طلبات ميدانية جديدة)

- ✅ **بند 41 — العمل بدون إنترنت (Offline Mode)** — PWA خفيف (Service
  Worker بنطاق الموقع كامل `/sw.js` + IndexedDB) يغطي شاشات الإدخال
  الميداني فقط: بلاغات العامل (بند 27/28)، والفحوصات/العلاجات/
  التطعيمات/السونار للطبيب والممرض. طابور محلي عام (`data-offline` على
  أي فورم) يحفظ أي حقل — نص أو ملف/صوت — ويزامن تلقائياً بثلاث طبقات
  احتياطية (`online` event، عند فتح الصفحة، ومحاولة كل 30 ثانية) لدعم
  iOS Safari اللي ما يدعم Background Sync. جلسة دخول دائمة
  (`remember=True`) ضرورية للمزامنة بعد إغلاق التطبيق. اللوحات
  الإدارية/المالية تبقى تحتاج إنترنت دائماً عمداً. اختُبر End-to-End
  فعلياً (نموذج نصي + نموذج فيه ملف صوتي، كلاهما زامن فعلياً للسيرفر عند
  محاكاة رجوع الاتصال) ونُظّف كل شي بعدها. راجع بند 41 بـ`MASTER_SPEC.md`.
- ✅ **بند 42 — تخصيص الحظائر والتنبيهات** — إصلاح فجوتين حقيقيتين بميزة
  "العامل المسؤول عن حظيرة" (كانت توجّه المهام تلقائياً من الأساس، بس):
  (1) شاشة تعديل حظيرة جديدة (`/barns/<id>/edit`) لتغيير المسؤول بعد
  الإنشاء (كان يُحدَّد مرة وحدة بس)، و(2) توجيه التنبيهات فعلياً —
  `barn_id` جديد بكل تنبيه + `get_alerts(barn_ids=...)` + شاشة
  `/alerts/mine` جديدة للعامل (بدون حاجة لصلاحية `animals.view`). اختُبر
  فعلياً بعاملين وحظيرتين منفصلتين (كل واحد شاف تنبيه حظيرته بس)، وبعد
  إعادة تعيين حظيرة من الأول للثاني تحرّك العدّاد حي وصحيح فوراً. راجع
  بند 42 بـ`MASTER_SPEC.md`.
- ✅ **بند 43 — قائمة الأمراض الشائعة وطرق الاستخدام/الجرعات** — جدول
  جديد `DiseaseType` (12 مرض شائع مزروع افتراضياً عبر `flask seed`)
  يغذّي `<datalist>` جديد بحقل "اسم المرض" (اختيار سريع أو كتابة حرة،
  بدون أي قيد). حقلان جديدان بـ`Pharmacy` (`usage_method`،
  `standard_dosage_note`) يُدخلهما الدكتور يدوياً مرة وحدة لكل دواء،
  وودجت مشتركة جديدة (`health/_medicine_widget.html`) تعرضهما فوراً عند
  اختيار الدواء بالزيارة البيطرية/المرض/التطعيم (بدون طلب شبكة إضافي).
  شاشة إدارة جديدة `/health/disease-types` — **أول استخدام فعلي لصلاحية
  `medical_options.manage`** اللي كانت مسجّلة بدون شاشة (فجوة اكتُشفت
  ببند 29). **قاعدة محترمة بالكامل**: كل هذا مرجع وصفي يعيد عرض ما كتبه
  الدكتور بنفسه — صفر حساب أو اقتراح جرعة تلقائي بأي مكان، نفس مبدأ
  "المساعد قرار مو طبيب". Migration حقيقية شُغّلت فعلياً (`flask db
  migrate`/`upgrade`). اختُبر فعلياً (datalist، صندوق معلومات الدواء،
  حفظ حالة مرضية كاملة، وفحص `medical_options.manage` كحاجز حقيقي –
  المالك ✅، ممرض تجريبي 403) ونُظّفت بيانات الاختبار مع الإبقاء على
  الاثني عشر مرضاً المزروعة (بيانات مرجعية حقيقية). راجع بند 43
  بـ`MASTER_SPEC.md`.
- ✅ **بند 44 — دعم اللغات الميدانية** — فعّل `Flask-Babel` (كان سقالة
  فاضية من الأساس، بند 3.2). نطاق مطابق تماماً لبند 41 (العامل/الطبيب/
  الممرض فقط، بقرارك الصريح): نفس الثمانية شاشات ميدانية + شريط التنقل
  والدخول. 112 نص واجهة استُخرجت وتُرجمت كاملة لثلاث لغات (إنجليزي/
  أمهرية/هندية) — `app/translations/{en,am,hi}`. مبدّل لغة ذاتي لأي
  مستخدم (`/settings/language`)، ومبدّل بشاشة الدخول نفسها قبل تسجيل
  الدخول (`/login/language`، عشان عامل غير قارئ للعربي يقدر يقرأ حقول
  الدخول). `<html lang dir>` يتغيّر ديناميكياً (الأمهرية/الهندية LTR
  زي الإنجليزي). **قاعدة صارمة**: القيم المخزَّنة فعلياً بقاعدة البيانات
  (نوع البلاغ، اسم المرض، نتيجة السونار) تبقى عربية ثابتة عمداً — فقط
  واجهة الإدخال تُترجَم، عشان شاشة الدكتور تبقى قيمة واحدة متسقة بغض
  النظر عن لغة رافع البلاغ. اللوحات الإدارية/المالية تبقى عربي فقط —
  قرار نطاق صريح مطابق لبند 41. اختُبر فعلياً بالمتصفح للثلاث لغات
  (لقطات شاشة، تحقق بصري من الخط والاتجاه)، مبدّل الدخول قبل تسجيل
  الدخول، وأولوية `User.language` على اختيار الجلسة المؤقت. نظّفنا
  بيانات الاختبار بعدها. راجع بند 44 بـ`MASTER_SPEC.md`.
- ✅ **بند 45 — سلامة الإدخال الإجباري + تكلفة الرأس الفردية** — عمل
  جلسة سابقة، أكّدته هذي الجلسة بفحص الكود (لا يوجد test suite آلي
  بالمشروع). فحص خادم حقيقي (مو JS بس) يمنع رفع بلاغ بدون حيوان ولا
  حظيرة، و`required` على حقول أساسية بعدة نماذج صحية/بلاغات. صفحة
  تفاصيل الرأس صارت تعرض **تكلفة الرأس الفردية** تلقائياً: شراء +
  تكاليف طبية مباشرة + تقدير علف (موصوف كتقدير صراحة، بدون تتبّع تاريخي
  لتنقّل الحظائر) = إجمالي تقديري. تكاليف الحظيرة العامة غير مُتضمَّنة
  عمداً (غير متتبَّعة بالنظام). راجع بند 45 بـ`MASTER_SPEC.md`.
- ✅ **بند 17 — إكمال محرك العمليات الجماعية بالكامل** — بطلبك الصريح
  (2026-07-23)، وسّعنا المحرك من أربع لـ**تسع عمليات**: بيع (محمي ببوابة
  الدورة)، تسجيل نفوق (بدون بوابة)، علاج/مرض (بحارس سلامة بيانات بند
  45)، عزل (نقل تلقائي لحظيرة العزل)، فحص سونار (نتيجة مستقلة لكل رأس)
  — إضافة للأربعة الأصلية (وزن/تحصين/ملاحظة/نقل حظيرة). زودنا شاشة
  "شراء دفعة جديدة" منفصلة (`/animals/bulk-purchase`). التغذية الجماعية
  ما احتاجت دالة جديدة (`FeedMovement` أصلاً حركة بمستوى الحظيرة
  الكاملة). اختُبرت التسع عمليات End-to-End فعلياً ببيانات تجريبية
  حقيقية (دفعة شراء 4 رؤوس، عزل جماعي، سونار جماعي، علاج جماعي —
  بمسار رفض ومسار نجاح، بيع جماعي مرفوض صح ببوابة الدورة، نفوق جماعي
  ناجح) ونُظّفت بالكامل بعدها — بما فيها بقايا اختبار قديمة من جلسة
  سابقة (`TEST-COST-B`) كانت موثّقة بالخطأ كمنظّفة، اكتُشفت ونُظّفت
  الآن. راجع بند 17 بـ`MASTER_SPEC.md`.
- ✅ **بند 46 — تدقيق شامل على خمسة أقسام** (2026-07-24) — بطلبك
  "توجيه رئيسي وشامل" غطّى المواشي/الصيدلية/الأعلاف/المالية/الصلاحيات
  معاً. أهم قرارين احتجنا تأكيدك عليهما (ما لقيت توثيقاً سابقاً لهما):
  صيغة TEMP-ID (قررت: مبني على معرّف الأم) وآلية توزيع المصاريف غير
  المباشرة (قررت: قسمة متساوية عبر حاسبة التكلفة الشهرية/السنوية
  الموجودة أصلاً). البنود: (1) رقم مؤقت تلقائي للمواليد + أول شاشة
  تعديل حيوان بالنظام + `birth_date` يُفترض اليوم لو نُسي. (2) حظر سحب
  سالب من الصيدلية/الأعلاف (كان يُقصّ بصمت) + تنبيه فترة تحريم عند
  الحليب/البيع. (3) حظيرة إلزامية لحركة صادر العلف. (4) عمود
  `Finance.is_indirect` جديد + نصيب فردي بتقرير تكلفة الرأس + إجمالي
  سنوي بالتقرير المالي. (5) تقييد العامل الميداني بحظائره (فلترة +
  فحص خادم حقيقي) + بادج مهام متأخرة + تأكيد إن كل السجلات الحساسة
  أصلاً Soft Delete فقط. اختُبرت الخمسة End-to-End فعلياً (تفاصيل
  كاملة ببند 46 بـ`MASTER_SPEC.md`) ونُظّفت بيانات الاختبار بعدها —
  بما فيها بقايا مكوّن علف قديم من جلسة سابقة اكتُشفت أثناء التنظيف.
- ✅ **بند 47 — اختبارات آلية (pytest) + فلترة حظيرة كاملة** (2026-07-24)
  — أول مجموعة اختبارات آلية بالمشروع: **67 اختباراً صفر فاشل**
  (`tests/` + `pytest.ini` + `pytest` بـ`requirements.txt`)، قاعدة
  بيانات SQLite مؤقتة منفصلة تماماً عن الفعلية. تغطي المنطق المُضاف/
  المُصلَح ببنود 17/45/46 تحديداً: TEMP-ID، معالجة سهو التواريخ، حظر
  السحب السالب (صيدلية/علف)، فترة التحريم، حظيرة إلزامية لصادر العلف،
  توزيع المصروف غير المباشر، **التسع عمليات الجماعية كاملة** (نجاح/
  تخطّي/رفض رأس واحد بس مع استمرار الباقي)، وتقييد العامل بحظائره
  (فحص فعلي عبر Flask test client). راجعت "دفعة أو حظيرة كاملة" ببند
  17 — الأنشطة الخمسة (بيع/شراء/تحصين/نقل/عزل) كانت مبنية فعلاً من
  قبل، الفجوة الحقيقية كانت طريقة تحديد حظيرة كاملة بضغطة — أضفنا
  فلترة `/animals` بحظيرة، فيصير "تحديد الكل" الموجود أصلاً = تحديد
  كل رؤوس الحظيرة تلقائياً. راجع بند 47 بـ`MASTER_SPEC.md`.
- ✅ **بند 48 — المساعد التشخيصي + محرك التغذية والتنبؤ** (2026-07-24)
  — قبل البناء طرحت قلقاً صريحاً إن "شجرة قرار تصل لتشخيص دقيق" تتعارض
  مع مبدأ "المساعد قرار مو طبيب" (بنود 13/25/43)؛ بعد الشرح قررت صراحة
  إنك تبي تشخيصاً فعلياً — التزمنا بقرارك مع الحفاظ على شبكة أمان
  بنيوية (أي سجل مرضي من المساعد يبقى "مفتوح" لازم دكتور فعلي يراجعه
  ويغلقه، قاعدة 12.3 الموجودة أصلاً). **المساعد التشخيصي** (قسم
  الصحة/الصيدلية): شجرة قرار تفاعلية حقيقية (`Symptom`+`DiseaseSymptomLink`
  جديدان، 30 عرض/41 رابط)، محرك تطابق يرتّب الاحتمالات ويعبّي شاشة
  تسجيل المرض تلقائياً؛ فحص مخزون تلقائي + شاشة نواقص صيدلية باقتراح
  بدائل بنفس التصنيف (عمود `Pharmacy.min_stock_qty` جديد)؛ دليل حقن
  ميداني عام (ست طرق) مدمج بودجت الدواء المشترك. **محرك التغذية
  والتنبؤ** (قسم الأعلاف): FCR فعلي من حركات العلف + سجل الأوزان؛
  موازِن عليقة **ببرمجة خطية حقيقية** (`scipy.optimize.linprog`، حزمة
  جديدة) يبني خلطة من الصفر بأقل تكلفة — الاختبارات الآلية اكتشفت
  وأصلحت خللاً حقيقياً بالسقف الأقصى لكل مكوّن كان يخلي حلولاً ممكنة
  "مستحيلة" رياضياً؛ مؤشر بيع داخلي (تكلفة حدية مقابل متوسط تاريخي،
  صفر بيانات سوق خارجية بقرارك) مدمج بمحرك البيع الذكي (بند 19). 33
  اختباراً آلياً جديداً (86 إجمالي، صفر فاشل)، واختُبرت الشاشات فعلياً
  بالمتصفح End-to-End. راجع بند 48 بـ`MASTER_SPEC.md` للتفاصيل الكاملة.
- ✅ **بند 49 — رادار الجفاف والإجهاد الحراري المناخي** (2026-07-25) —
  أول اتصال HTTP خارجي فعلي بالمشروع، على 4 قرارات معمارية أجبتها
  كلها بالخيار الموصى به: **Open-Meteo** (مجاني بدون مفتاح) لموقع
  **واحد يمثّل المزرعة كلها** (`FarmSettings.farm_latitude/longitude`
  جديدان)، مؤشر **THI** (معادلة NRC القياسية للأبقار الحلوب — تقريب
  عام موثّق صراحة، مو معياراً رسمياً للأغنام/الماعز) بساعة الذروة
  الحرارية اليومية مو متوسطاً مسطّحاً. عند إجهاد **متوسط فأعلى** تتولّد
  تلقائياً 4 مهام تفقّد لكل حظيرة **فوق نظام Task الموجود أصلاً**
  (تعديل مواعيد الإعلاف، إضافة مضاد إجهاد حراري بالماء "إلزامي"، فحص
  تهوية، فحص مظلات) — بحالة "مقترحة" بانتظار مراجعة الدكتور، نفس
  اتفاقية بقية المهام الآلية بالنظام (تناسق متعمّد، مو قناة دفع فورية
  جديدة)، مع اتقاء تكرار صريح بالحظيرة/التاريخ. **مؤشر ربط الاستهلاك
  بـFCR**: يقارن كجم علف/رأس/يوم بآخر 3 أيام حر بمتوسط 14 يوماً أهدأ
  قبلها — **توصية فقط يراجعها المالك** (بقرارك الصريح)، صفر تعديل
  تلقائي بالعليقة. صلاحيتان جديدتان (`climate.view`/`climate.manage`).
  18 اختباراً آلياً جديداً (104 إجمالي، صفر فاشل) — بما فيها جلب
  Open-Meteo مع تزييف `requests.get` واتقاء التكرار. اختُبر فعلياً
  **باتصال حقيقي بـOpen-Meteo** (لا تزييف) بإحداثيات تجريبية أعطت
  توقّع 7 أيام فعلي وتولّدت 4 مهام تفقّد صحيحة، ثم نُظّفت البيانات
  التجريبية بالكامل وأُعيد ضبط موقع المزرعة لفاضٍ — الميزة معطّلة الآن
  لين المالك يدخل إحداثياته الحقيقية. راجع بند 49 بـ`MASTER_SPEC.md`
  للتفاصيل الكاملة.
- ✅ **بند 50 — تحديث الأتمتة (مهام/مخزون) + أمان طبي وفترة التحريم**
  (2026-07-25) — تدقيق أثبت إن الخصم أصلاً ما يصير عند إنشاء مهمة (كان
  دائماً مقيّداً بتسجيل فعلي)؛ الفجوة الحقيقية كانت غياب رابط
  مهمة↔تسجيل. بُنيت مهام **"علاج مخطَّط"** جديدة (دواء+جرعة مخزَّنة على
  المهمة، صفر خصم بحد ذاتها) + شاشة تفصيل شامل جديدة (`/team/tasks/
  <id>`: سبب، حظيرة/عدد رؤوس الدفعة عبر تجميع مهام بمصدر مشترك — بدون
  نموذج Batch جديد بقرارك، دواء/جرعة إجمالية، مخزون حالي/متوقع، المهمة
  التالية بالسلسلة) + زر **"تأكيد التنفيذ"** يفتح اختصاراً معبّى مسبقاً
  لنموذج التسجيل الحقيقي الموجود أصلاً — هناك فقط يصير الخصم، وعندها
  تلقائياً تُنجَز المهمة **وتُجدوَل متابعة إعادة وزن** (`FarmSettings.
  reweigh_followup_days`) تبدأ تُحسب من لحظة التنفيذ الفعلية بالضبط.
  شاشة **خطة علاج جماعي** جديدة (ضمن العمليات الجماعية الموجودة) هي
  نقطة الدخول الفعلية. **حارس إعادة الجرعة**: `Pharmacy.medicine_class`
  حقل جديد بقائمة ثابتة (منفصل عن `category` النصي)، يحذّر عند تكرار
  جرعة "مضاد طفيليات" خلال 30 يوماً (`FarmSettings.antiparasitic_
  redose_days`) مع تجاوز بسبب صريح يُسجَّل بـAuditLog — بقرارك، تحذير
  لا حظر نهائي. **فترة التحريم**: تحوّلت من تحذير بعد البيع (بند 46)
  لحظر حقيقي قبله عبر بوابة `CycleExitBlocked` الموجودة أصلاً (نقطة
  دخول واحدة تغطي البيع الفردي والجماعي)، مع عداد أيام متبقية حقيقي
  ورفع تلقائي فور الانتهاء (محسوب حياً، صفر عمود حالة أو مهمة مجدولة).
  16 اختباراً آلياً جديداً (120 إجمالي، صفر فاشل). اختُبر فعلياً
  بالمتصفح End-to-End بالكامل: خطة جماعية → تفصيل شامل مطابق → تأكيد
  تنفيذ → خصم حقيقي (50→48) + تحريم صحيح (15 يوماً) → إنجاز تلقائي +
  متابعة وزن مجدولة → عداد تحريم صحيح بصفحة الحيوان → حارس تكرار رفض
  بدون سبب ونجح بسبب (+AuditLog). عُثر أثناء الاختبار على خلل بيانات
  قديم غير مرتبط (`FeedRationItem` يتيم من جلسة سابقة) ونُظّف، وكل
  بيانات الاختبار نُظّفت بالكامل بعدها. راجع بند 50 بـ`MASTER_SPEC.md`
  للتفاصيل الكاملة.
- ✅ **بند 51 — المحرك الطبي/التغذوي/العملياتي الذكي** (2026-07-25) —
  6 معايير بيطرية حُوِّلت لقواعد أتمتة. **البند السادس كان مبنياً
  بالكامل ببند 50 بنفس اليوم** (حظر تكرار الطفيليات + قفل التحريم
  بعداد تنازلي) — تحقّقنا بالكود قبل البناء ووثّقنا هذا صراحة بدل
  التكرار. الباقي: **أول استخدام فعلي** لـ`pre_birth_feed_change_days`
  (كان عموداً بالإعدادات بدون منطق يقرأه) — مهمة تلقائية عند الثلث
  الأخير (بروتين أعلى + سيلينيوم/E/AD3E)؛ حظر الزيادة المفاجئة
  للمركزات (حقل `Feed.feed_class` جديد) ونسبة كالسيوم:فسفور 2:1 —
  كلاهما تحذير+تجاوز بسبب صريح؛ **حظر نحاس النعيمي** — حقل `Animal.
  breed` جديد (أول تمييز سلالة بالنظام) + `contains_high_copper` على
  الدواء/العلف، **حظر برمجي صريح بلا تجاوز** (يخالف نمط بقية حراس
  هذا البند، بقرارك) مقصور على سجلات مرتبطة برأس محدد؛ **بروتوكول
  الإجهاض** — حقول `Pregnancy.outcome` جديدة + زر "تسجيل إجهاض" يعزل
  فوراً + مهمة سحب عيّنات + مهمة مراقبة حرارة واحدة (مو 14 يومية) لكل
  رفيق حظيرة؛ **بروتوكول الطوارئ** — عرض جديد "عمى مفاجئ/عتامة العين"
  بالمساعد التشخيصي يعزل تلقائياً ويعرض تشخيصاً تفريقياً (ليستريا/
  B1/Pinkeye)؛ **تأخر الشياع** كتنبيه مستقل (كان مطموراً بمحرك البيع
  الذكي)؛ **رعاية المولود الأولية** — 3 مهام جديدة (تعقيم سرة/سيلينيوم/
  تأكد رضاعة) بخطة العزل بعد الولادة. 19 اختباراً آلياً جديداً (139
  إجمالي، صفر فاشل). اختُبر حياً بالمتصفح — بروتوكول الطوارئ اشتغل
  بالكامل (عزل فعلي فوري + تشخيص تفريقي)، حقل السلالة الجديد ظهر
  بنموذج الحيوان، شاشة تشخيص الحمل تعرض زر الإجهاض بدون أخطاء. راجع
  بند 51 بـ`MASTER_SPEC.md` للتفاصيل الكاملة.

- **بند إضافي 52 (2026-07-25)** — التفاصيل الكاملة اللي كانت مفتوحة
  منذ بند 17 (قوالب بروتوكول، مسار استقبال دفعة، تعدد مستودعات) وصلت
  وبُنيت كاملة، بعد 3 قرارات معمارية حقيقية طُرحت وأُجيب عليها. **قوالب
  بروتوكول العلاج** — `TreatmentProtocol`/`TreatmentProtocolStep` جديدان،
  التطبيق يعيد استخدام آلية "علاج مخطَّط" ببند 50 حرفياً (مهمة مقترحة
  لكل خطوة، خصم المخزون وفترة السحب الأطول يصيران فقط عند تأكيد التنفيذ
  الفعلي — صفر كود إضافي لهذا الجزء). **مسار استقبال دفعة جديدة** —
  `AnimalBatch` جديد + 3 مراحل (حجر وأوليات → ترقيم/فحص → توزيع نهائي)
  عبر بلوبرنت `app/batches/` جديد، تقدّم جماعي بضغطة زر مع **استبعاد/
  عزل فردي** لرأس مشتبه بها (إضافتك الصريحة أثناء الحوار) يستثنيها من
  التقدّم الجماعي وتلحق فردياً لاحقاً. **تعدد المستودعات** — طبقة إضافية
  فوق `available_qty` الإجمالي (قرارك الصريح: صفر تعديل على ~10 دوال
  مُختبرة أصلاً)، المستودع الافتراضي محسوب بالطرح لا مخزَّناً، والتحويل
  الصريح بين مستودعين لا يغيّر الإجمالي إطلاقاً. 22 اختباراً آلياً جديداً
  (161 إجمالي، صفر فاشل). اختُبرت الشاشات الثلاث حياً بالمتصفح بالتسلسل
  الكامل من إنشاء بروتوكول وحتى تحويل مخزون بين مستودعين. راجع بند 52
  بـ`MASTER_SPEC.md` للتفاصيل الكاملة.

- **بند إضافي 53 (2026-07-25، ✅ مكتمل ومختبر) — توسيع العمل بدون إنترنت
  ليشمل النظام بالكامل.** طلب صريح: توسيع طابور المزامنة المحلي
  (المبني ببند 41) من 8 شاشات إدخال فردي بسيطة إلى **كل شاشات النظام
  بما فيها العمليات الجماعية والمالية**، ولكل الأدوار (مو بس عامل/طبيب/
  ممرض)، مع نسخة محفوظة للتقارير القرائية. قبل التوسيع، اكتُشفت ثغرة
  أمان بيانات حقيقية بالآلية القديمة: `flushQueue()` كان يعتبر أي رد
  HTTP 200 "نجاح" ويحذف من الطابور — لكن نمط هذا المشروع الموحّد بكل
  الراوتات (فشل التحقق = flash + إعادة توجيه لنفس صفحة النموذج؛ نجاح =
  إعادة توجيه لصفحة مختلفة) يعني إن أي طلب مرفوض تحقّقياً (نفاد مخزون،
  حظر نحاس، حيوان مو مؤهَّل...) كان سيُحذَف من الطابور بصمت وكأنه نجح
  — **خطر حقيقي لو تُرك كما هو وتوسّع للعمليات الجماعية/المالية**. صار
  الإصلاح أساس التوسيع بدل تكرار نفس الثغرة على نطاق أوسع:
  - **جزء أ (الأساس الآمن)**: `offline_sync.js` صار يقارن الرابط
    النهائي بعد المزامنة (`res.url`) بالرابط الأصلي — لو رجع لنفس
    صفحة النموذج (نمط الفشل الموحَّد بكل المشروع، صفر تعديل على أي
    راوت) يُصنَّف "فشل تحقّق" ويبقى ظاهراً بلوحة مراجعة جديدة (مو
    حذفاً صامتاً ولا إعادة محاولة لا نهائية)، بينما فشل الشبكة الفعلي
    يبقى بالطابور لإعادة المحاولة التلقائية العادية.
  - **جزء ب**: إزالة قيد الأدوار الثلاثة (`base.html`) — كل الأدوار
    تحمّل الـService Worker وتقدر تثبّت التطبيق الآن؛ توسيع قائمة
    الصفحات المخزَّنة وإضافة `data-offline` لكل نماذج التسجيل الفردي
    بكل الوحدات.
  - **جزء ج**: العمليات الجماعية والمالية — تستفيد تلقائياً من إصلاح
    جزء أ (نفس نمط الفشل-يرجع-لنفسه)؛ **قرار مقبول صراحة منك بعد
    تنبيه**: ما فيه فحص تعارض حقيقي بين جهازين أوف لاين لنفس المخزون/
    المبلغ لحظة الإدخال — الحراس الموجودة أصلاً بالسيرفر (منع سحب
    سالب، بوابات دورة الإنتاج، حظر النحاس...) هي خط الدفاع الفعلي،
    ولوحة المراجعة الجديدة (جزء أ) هي آلية اكتشاف الرفض بعد المزامنة،
    مو منعه وقت الإدخال.
  - **جزء د**: التقارير/اللوحات القرائية تُضاف لقائمة التخزين المؤقت
    بنفس استراتيجية "شبكة أولاً، نسخة محفوظة كبديل" الموجودة أصلاً —
    بانر صريح "نسخة محفوظة" يظهر وقت عدم الاتصال.

  اختُبر End-to-End فعلياً بالمتصفح (محاكاة `navigator.onLine`/أحداث
  `online`/`offline`): مسار نجاح عادي (دواء أوف لاين اتسجّل وظهر فعلياً
  بالسيرفر بعد المزامنة)، ومسار الرفض — **الاختبار الحاسم** — اسم مرض
  مكرَّر اتسجّل أوف لاين، رفضه السيرفر عند المزامنة، والنظام صنَّفه
  "فشل" صح بدل حذفه بصمت (ظهر بلوحة المراجعة، والقائمة بالسيرفر ما
  تكرّرت). تأكيد تحميل الـmanifest لحساب مو عامل/طبيب/ممرض. 161 اختبار
  pytest موجود سابقاً بقي ناجحاً بالكامل (صفر تراجع — البند JS/قوالب
  فقط). راجع بند 53 بـ`MASTER_SPEC.md` للتفاصيل الكاملة.

راجع القائمة المرقّمة الكاملة آخر `MASTER_SPEC.md` لكل بند بالتفصيل.
صفر بنود رئيسية بحالة ❌ متبقية بكل أقسام الوثيقة الخمسة — كل بند إما
✅ منجز ومختبر فعلياً، أو 📌/🚫/⏭️/🕰️ بوضوح لسبب موثّق (مبدأ عمل مُستوعب،
مرفوض صراحة بقرارك، غير منطبق هيكلياً، أو مرجع تاريخي متجاوز). فيه
نقطتان فرعيتان موسومتان ❌ داخل بند 3 التاريخي (أعلى مستوى معلَن من
قبل) — موثّقتان ومو مخفيتان، وما تُغيّران حالة أي بند رئيسي. **بند 17
صار ✅ مكتملاً بالكامل** بعد ما كان 🟡 جزئياً. كل بند من 25 إلى 34 اختُبر
End-to-End فعلياً ببيانات تجريبية حقيقية، ونُظّفت جميعها بالكامل بعد كل
اختبار.

## الهوية البصرية

- المستخدم زوّدنا بملف `Index.html` من محاولة سابقة (تصميم واجهة موبايل
  كامل بألوان دافية بنّية/كريمية، بطاقات مدوّرة، تبويبات حبوب). اعتمدنا
  نفس نظام الألوان والمكوّنات (`.card`, `.btn`, `.badge`, `table`,
  `.workflow-map`, `.gate-row`...) داخل `app/templates/base.html`، بدون
  تغيير بنية القوالب الموجودة — معظم الشاشات ترقّت بصرياً تلقائياً لأنها
  تستخدم نفس أسماء الوسوم (`table`, `input`, `label`) والفئات (`card`,
  `btn`, `badge`) أصلاً.
- صفحة "دورة الإنتاج" (`animal_workflow.html`) أعيد بناؤها خصوصاً لتستخدم
  `.workflow-map`/`.workflow-stage` (خط سير مرئي بالمراحل) و`.gate-list`/
  `.gate-row.fail` (المتطلبات الناقصة) — نفس مكوّنات تصميم المستخدم بالضبط.
- **لم نعتمد** الجزء التطبيقي من ملف Index.html (كان صفحة SPA واحدة بجافاسكربت
  تبدّل الشاشات، على عكس معمارية Flask متعددة الصفحات المعتمدة هنا) — بس
  الهوية البصرية (CSS) فقط.

## قرارات معمارية اتُّخذت (مرجع)

- محرك دورة الإنتاج: جدول منفصل (`ProductionWorkflow`) + سجل أحداث
  (`CycleEvent`) يُشتق منه المرحلة تلقائياً، مو حقل واحد على الحيوان.
- بيع الحيوان مسار واحد محمي ببوابة اكتمال الدورة؛ **النفوق بدون بوابة**
  (حقيقة واقعية لا قرار عمل)؛ الأرشفة محمية مثل البيع.
- إلغاء عملية بيع يرجّع الحيوان لآخر مرحلة مؤكّدة تلقائياً (مو يعلّق على
  مرحلة 10).
- مسار الحيوان بمحرك الدورة (تربية/تسمين/فحل...) يُحدَّد **مرة واحدة**
  ويبقى ثابت، لا يُعاد حسابه تلقائياً لو تغيّر الغرض لاحقاً.
- التحويل ببلاغات الفريق يقتصر على أعضاء عندهم حساب دخول فقط — سجل
  الأطباء الخارجيين (`Doctor`) منفصل تماماً ولا يظهر بقائمة التحويل.
- إغلاق البلاغ حصري لنفس الشخص اللي استلمه (`manager_id`)، بغض النظر عن
  صلاحياته الأخرى — قيد مطبّق بالكود، مو بقاعدة البيانات مباشرة (نقطة
  تستاهل قيد DB حقيقي لو انتقلنا لإنتاج فعلي).
- **إصلاح (2026-07-22)**: "الترتيب غير المنتظم" بمحرك الدورة كان يُحتسب
  حتى من مراحل قائمة على غياب مؤشرات سلبية (تخطيط السوق، التقييم) بدل
  دليل فعلي — كان يعطي نتائج خاطئة على حيوانات جديدة تماماً. صار يُحتسب
  بس من مراحل الدليل الفعلي (تلقيح/تشخيص حمل/إدارة حمل/ولادة). وصار
  العدّاد يزيد مرة وحدة عند أول اكتشاف فقط، مو بكل مرة تُفتح صفحة الحيوان.

## قرارات اتُّخذت افتراضياً (بدل التوقف والانتظار)

بطلب صريح منك بتاريخ 2026-07-22 ("خلّني أكمل بشكل متواصل")، صرت آخذ قرار
افتراضي معقول على أي نقطة مفتوحة غير حرجة وأكمل، بدل ما أوقف وأسأل:

- **تسلسل 4/5/6**: بنيت نظام مهام مبسّط (سيرفر عادي، بدون أوف لاين) بدل
  انتظار تطبيق الـPWA — هذا خدم المرحلة 4 والنص الأول من المرحلة 6 فوراً.
- **محرك الدورة (كود مقابل بيانات)**: خليته بالكود الحالي (مجرّب وشغّال)،
  ما أعدت بناءه كجدول بيانات.
- **أرقام العزل الافتراضية** (7 أيام، 48 ساعة، 21 يوم) — قيم اجتهادية،
  موثّقة بالكود، قابلة للتغيير لو أعطيتني أرقام حقيقية.

## قرارات مفتوحة لسا بانتظار الرد

1. ✅ **حُلّت بالكامل (2026-07-25)** — مواصفة "العمليات الجماعية"
   الأساسية (بيع/شراء/تحصين-علاج/عزل/نقل حظيرة/سونار جماعي) بُنيت
   ببند 17. الجزء اللي بقي مفتوحاً — قوالب علاج جاهزة، مسار استقبال
   قطيع بمراحل، ربط مستودعين فعليين — وصلت تفاصيله الكاملة وبُني
   كاملاً ببند إضافي 52. **صفر نقاط مفتوحة من هالقائمة الآن.**
