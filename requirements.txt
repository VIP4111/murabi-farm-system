Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.0.7
python-dotenv==1.0.1
Werkzeug==3.0.3
email-validator==2.2.0
reportlab==5.0.0
arabic-reshaper==3.0.1
python-bidi==0.6.11
openpyxl==3.1.5
Flask-Babel==4.0.0
qrcode==7.4.2

# حماية CSRF (بند إضافي 93، 2026-08-02) — قبل هذا البند ما كان فيه أي
# رمز CSRF بأي فورم بالمشروع إطلاقاً.
Flask-WTF==1.3.0

# اختبارات آلية (بند إضافي، 2026-07-24) — pytest فقط، بدون plugins إضافية
pytest==9.1.1

# موازِن العليقة (بند إضافي، 2026-07-24) — scipy.optimize.linprog لحل
# مسألة "خلطة العلف الأرخص" (Diet Problem الكلاسيكية) كبرمجة خطية حقيقية،
# بدل تخمين يدوي. numpy مثبَّت بنسخة صريحة (بند إضافي 193) بدل ما
# تكون تبعية ضمنية غير مثبَّتة — لمنع pip من اختيار نسخة numpy قد لا
# تتوافق مع نسخة بايثون على سيرفر Render أثناء البناء.
numpy==2.5.1
scipy==1.18.0

# اختياري — يُثبَّت بس لو فعّلت ترقية المساعد الذكي لـClaude API (بند 25)
# بإضافة ANTHROPIC_API_KEY بملف .env. بدون المفتاح، المساعد يشتغل بمحرك
# محلي بالكامل بدون هذي الحزمة إطلاقاً.
# anthropic

# رادار المناخ والإجهاد الحراري (بند إضافي 49) — أول اتصال خارجي فعلي
# بالمشروع، لجلب توقعات الطقس من Open-Meteo (مجاني بدون مفتاح، بقرارك
# الصريح).
requests==2.34.2

# سيرفر إنتاج/اختبار (بند إضافي، 2026-07-25) — python run.py المدمج بـFlask
# للتطوير المحلي فقط؛ gunicorn هو اللي يشغّل التطبيق فعلياً بأي نشر حقيقي
# (Dockerfile/Procfile). راجع DEPLOY.md.
gunicorn==23.0.0

# قابلية الاتصال بـPostgreSQL (بند إضافي 77، 2026-08-01) — بدون هذا،
# لو ضُبط DATABASE_URL على رابط Postgres، SQLAlchemy يفشل بخطأ "no
# module named psycopg2" عند أول طلب. binary= بدون حاجة لأدوات بناء
# بسيرفر النشر (أبسط وأضمن من psycopg2 العادي على Render). بدون
# DATABASE_URL، التطبيق يستمر يشتغل بـSQLite كالمعتاد — هذا مجرد تجهيز.
psycopg2-binary==2.9.12

# جدولة تلقائية حقيقية (بند إضافي 78، 2026-08-01) — أول Cron حقيقي
# بالمشروع، يشغّل توليد المهام اليومية مرة كل يوم داخل عملية التطبيق
# نفسها (بدون خدمة Render منفصلة). راجع app/core/scheduler.py.
APScheduler==3.11.1

# تحويل ميلادي↔هجري دقيق (بند إضافي 255) — لشارت الموسمية (رمضان/عيد
# الأضحى)، بدل تقريب ميلادي غير دقيق. حساب رياضي محلي بالكامل، بدون
# أي اتصال خارجي.
hijridate==2.3.0
